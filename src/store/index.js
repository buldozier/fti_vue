import { createStore } from "vuex";
import programs from "@/store/modules/programs";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: { programs },
});
